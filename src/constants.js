/* ------------------------------------------------------------------ */
/*  layout constants                                                   */
/* ------------------------------------------------------------------ */

/** default width of a free-form text node */
export const NODE_W = 210;

/** default width of an image node */
export const IMG_W = 250;

/** horizontal gap between siblings in a child row */
export const GAP_X = 50;

/** vertical step used when nudging a new node out of a collision */
export const GAP_Y = 22;

/** vertical gap between a parent and its row of children */
export const CHILD_GAP_Y = 150;

/** how long the empty-canvas hint stays visible (ms) */
export const LANDING_MS = 5000;